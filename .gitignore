# Credenciais e segredos — nunca versionar (ver docs/RULES.md, seção 12.1)
*.env
.env*
*recovery*
*secret*
*token*
*credentials*
.git-incompleto-*/

# Dependências
node_modules/

# Build/cache
.next/
dist/
build/

# Testes e relatórios gerados localmente
test-results/
lighthouse-*.report.html
lighthouse-*.report.json
lighthouse-*_report.html
lighthouse-*_report.json

# Perfil temporário do Chrome usado pelo Lighthouse no WSL
C:*lighthouse.*/
