# Credenciais e segredos — nunca versionar (ver docs/RULES.md, seção 12.1)
*.env
.env*
!.env.example
*recovery*
*secret*
*token*
*credentials*
.git-incompleto-*/

# Dependências
node_modules/

# Configuração local dos agentes e logs efêmeros do preview
.claude/
server-*.log

# Build/cache
.next/
dist/
build/
next-env.d.ts
*.tsbuildinfo

# Testes e relatórios gerados localmente
test-results/
lighthouse-*.report.html
lighthouse-*.report.json
lighthouse-*_report.html
lighthouse-*_report.json

# Perfis temporários do Chrome gerados pelo Lighthouse (inclusive caminhos WSL/Windows malformados)
*lighthouse.[0-9]*/

.vercel

# Estado local temporário do Supabase CLI
supabase/.temp/

.env*
